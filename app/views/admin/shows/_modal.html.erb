<div class="modal" data-controller="modal" data-action="click->modal#close keyup@window->modal#closeWithEscape">
  <div class="modal-backdrop"></div>
  <div class="modal-panel" data-action="click->modal#stop">
    <div class="modal-header">
      <h2>New show</h2>
      <button class="icon-button" data-action="modal#close" aria-label="Close modal">×</button>
    </div>
    <div class="modal-body">
      <%= render "form", show: @show %>
    </div>
  </div>
</div>
